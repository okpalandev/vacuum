require("lib.matrix");
require("lib.vector");
require("lib.quaternion");

return {
    Vec2 = vector.Vec2,
    Vec3 = vector.Vec3,
    Vec4 = vector.Vec4,
    Matrix = matrix,
    Quaternion = quaternion
};

